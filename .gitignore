# Temporary files
*.tmp
*.log
*.bak
*~
.DS_Store

# Binary artifacts
*.xlsx

# Claude temporary files
tmpclaude-*

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/mcp.json
.idea/
*.swp
*.swo
*~

# OS files
Thumbs.db
.DS_Store

# Node modules (if any)
node_modules/
npm-debug.log*

# Python virtual environment
.venv/
venv/
env/

# Python cache (if any)
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so

# Build artifacts (if any)
dist/
build/
*.egg-info/

# CMake-generated files (machine-specific, regenerated via cmake ..)
src/mission/src/Makefile
src/mission/src/CMakeFiles/
src/mission/src/cmake_install.cmake
src/mission/src/CTestTestfile.cmake

# Local configuration
.env
.env.local
*.local

# Cross-process file lock files (created by JsonFileLock / Lock-JsonFile)
*.lock

# Agent output directories - tracked in Git (agent deliverables)
# NOTE: These directories are intentional deliverables created by agent workflows.
# They MUST NOT be gitignored or workflow jobs using 'git add' will fail.
# See: PM -> docs/prd/, Architect -> docs/adr/ + docs/specs/,
#      UX -> docs/ux/, DevOps -> docs/deployment/,
#      Reviewer -> docs/reviews/

# Cache directories
.cache/
.pytest_cache/
.mypy_cache/
