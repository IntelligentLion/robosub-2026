# Tells colcon to skip this directory.
#
# This is a second, heavier VSLAM implementation that duplicates
# src/localization/vslam_node.py. It is kept only as reference.
#
# Two reasons it must not build:
#   1. VSLAM is disabled for competition (see
#      src/localization/launch/vslam_localization_launch.py for rationale).
#   2. The directory name "zed2i_vslam(2)" contains parentheses, which
#      break colcon and most shell tooling. If you ever revive this package,
#      first rename the directory to a parenthesis-free name (e.g. zed2i_vslam).
